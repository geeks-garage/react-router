# ReactRouter
Demo application using React router

Running the app:
 - npm i
 - npm run dev
 - open localhost:8080 in browser

debug mode:
`npm run dev`

production mode:
`npm run prod`
open index.html
