import React from 'react';
import DefaultLayout from '../layouts/default';
import render from '../lib/render';

const Home = () => (
  <DefaultLayout>
    <h2>Home</h2>
  </DefaultLayout>
);

export default Home;
